/**
 * introduce of the package
 * <p>
 * Created by rongbin.xie on 2018/3/21.
 */
package lambda;

/**
 * More and more tranditional.bean are created with the design pattern is used. To add the code extensibility, we use the design the pattern,
 * however, in some way, we can not completely use the design the pattern correctly, sometimes, the usage of it will increase the
 * complexity. The balance between the two may a problem.
 *
 * However, now it's a good solution to use java function programing.
 *
 * 无论使用观察者模式或策略模式， 实现时采用 Lambda 表达式还是传统的类，
 * 取决于策略和观察者代码的复杂度。 我这里所举的例子代码很简单， 只是一两个方法调用，
 * 很适合展示新的语言特性。 然而在有些情况下， 观察者本身就是一个很复杂的类，
 * 这时将很多代码塞进一个方法中会大大降低代码的可读性。
 */
